# Email Configuration
# The DPS application supports Nodemailer for sending email notifications
# Configure the following variables based on your email provider

# Email transporter configuration
# For Gmail: Use "smtp.gmail.com" and enable "Less secure app access" or use App Passwords
# For other providers: Use your SMTP server details

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=noreply@dps.local

# Optional: Custom API URL for frontend
# VITE_API_URL=/api
